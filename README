= Ruby/PureData
Ruby library to manipulate PureData (Pd-extended) via socket.

== Requirements
  Mac OS X
  Pd-extended

== Synopsis

  require "ruby-puredata"

  Pd.start do |pd|
    osc = pd.obj("osc~", 440)
    mul = pd.obj("*~", 0.1)
    dac = pd.obj("dac~")
    pd.connect(osc.outlet, mul.inlet)
    pd.connect(mul.outlet, dac.left)
    pd.connect(mul.outlet, dac.right)
    pd.dsp = true
    sleep 5
    pd.dsp = false
  end

== Description

== Installation

== ToDo


